/**
 * Summary
 * Js dynamically typed or statically typed: dynamically because the type of the values can be determined during run-time
 * 
 */

const id = 123; //the type of id is number here but not required to be specified -- dynamically typed reason
console.log(typeof(id)); //number
